<table border="2">
    <tr>
        <td>id</td>
        <td>nom</td>
      
      
        <td>action</td>
    </tr>
    {% for i in room %} 
    <tr>
        <td>{{ i.id }}</td> 
        <td>{{ i.nom }}</td>
        
       
        
        <td><a href="{{ path('modifroom',{'id':i.id}) }}">modifier</a></td>
        <td><a href="{{ path('Deleteroom',{'id':i.id}) }}">delete</a></td>
    </tr>
    {% endfor %}
</table>







</table>