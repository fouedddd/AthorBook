<table border="2">
    <tr>
        <td>id</td>
        <td>nom</td>
        <td>model</td>
      <td>Room</td>
        <td>action</td>
    </tr>
    {% for i in car %} 
    <tr>
        <td>{{ i.id }}</td> 
        <td>{{ i.nom }}</td>
        <td>{{ i.model }}</td>
        <td>{{i.room}}</td>
       
        
        <td><a href="{{ path('modifcar',{'id':i.id}) }}">modifier</a></td>
        <td><a href="{{ path('Deletecar',{'id':i.id}) }}">delete</a></td>
    </tr>
    {% endfor %}
</table>







</table>